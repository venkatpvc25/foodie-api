package com.pvc.foodie.comman.exception;

public enum ErrorCode {

    USER_NOT_FOUND,
    INVALID_CREDENTIALS,
    TOKEN_EXPIRED,
    TOKEN_REVOKED,
    ACCESS_DENIED,
    RATE_LIMIT_EXCEEDED,
    VALIDATION_ERROR,
    RESOURCE_NOT_FOUND,
    INTERNAL_ERROR
}
